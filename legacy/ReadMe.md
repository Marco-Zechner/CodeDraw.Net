Add ".LegacyX" to AssemblyName and RootNamespace in .csproj when moving them here. ? .... i will try without first

Also remember to check the project references if you still use "active" libs. (probably need to add `..\..\`) at the front
